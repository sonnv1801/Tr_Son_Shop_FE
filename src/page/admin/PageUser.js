import React from 'react';
import { PageUsers } from '../../components/admin/PageUsers/PageUsers';

export const PageUser = () => {
  return (
    <div
      style={{
        marginTop: '104px',
        textAlign: 'center',
        textTransform: 'uppercase',
      }}
    >
      <PageUsers />
    </div>
  );
};
